// Placeholder — replaced in Phase 8

export default function AdminPage() {
  return (
    <div>
      <h1
        className="text-2xl font-bold mb-2"
        style={{ fontFamily: 'var(--font-space)', color: 'var(--text-1)' }}
      >
        Admin Overview
      </h1>
      <p className="text-sm mb-8" style={{ color: 'var(--text-2)' }}>
        Manage components, pricing, content — Phase 8.
      </p>

      <div
        className="flex items-center justify-center h-48 rounded-2xl text-sm"
        style={{
          background: 'var(--surface-1)',
          border:     '1px dashed var(--border)',
          color:      'var(--text-3)',
        }}
      >
        Admin dashboard content — Phase 8
      </div>
    </div>
  )
}